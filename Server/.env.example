# Render environment variables (set these in Render dashboard)
MONGOURL=mongodb+srv://<user>:<pass>@<cluster>/<db>?retryWrites=true&w=majority
JWT_SECRET=replace_with_a_strong_secret

# Comma-separated allowed frontend origins for CORS + Socket.IO
# Example:
# CLIENT_ORIGIN=http://localhost:5173,https://your-frontend.vercel.app
CLIENT_ORIGIN=http://localhost:5173
